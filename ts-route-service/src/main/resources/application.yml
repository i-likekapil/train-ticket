spring:
  application:
    name: ts-route-service
  data:
    mongodb:
      host: ts-route-mongo
#      host: localhost
#      username: user
#      password: ${MONGODB_PASSWORD}
      database: ts
      port: 27017
#      connectionTimeout: 2
#      socketTimeout: 2

# HTTP Server
server:
  port: 11178  # HTTP (Tomcat) port

swagger:
  controllerPackage: route.controller

logging:
  file: /var/log/ts-application.logs